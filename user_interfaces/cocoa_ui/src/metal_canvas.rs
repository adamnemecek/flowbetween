///
/// Represents a canvas that is drawn on using Metal (via gfx) rather than the Quartz drawing operations
///
pub struct MetalCanvas {

}

impl MetalCanvas {
    ///
    /// Creates a new metal canvas
    ///
    pub fn new() -> MetalCanvas {
        MetalCanvas {

        }
    }
}